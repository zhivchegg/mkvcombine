# MKV Merger Pro — Environment Configuration
# Copy to .env and adjust paths

# Path to video files directory
VIDEO_DIR=/path/to/your/videos

# Path to output directory (merged files)
OUTPUT_DIR=/path/to/your/output

# Web UI port (default: 5000)
PORT=5000
